[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && daphne -b 0.0.0.0 -p ${PORT:-8000} grocery_store.asgi:application"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

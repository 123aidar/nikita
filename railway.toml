[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && daphne -b 0.0.0.0 -p $PORT grocery_store.asgi:application"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
